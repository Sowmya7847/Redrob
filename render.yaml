services:
  - type: web
    name: redrob-ai-talent-intelligence
    env: python
    plan: free
    buildCommand: |
      pip install --upgrade pip
      pip install -r requirements.txt
    startCommand: |
      streamlit run app.py --server.port=$PORT --server.address=0.0.0.0
